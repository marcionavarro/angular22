import { CardNoFormatterPipe } from './card-no-formatter-pipe';

describe('CardNoFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardNoFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
